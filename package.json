{
  "name": "qasasforkids",
  "private": true,
  "scripts": {
    "build:dev": "NODE_ENV=dev node scripts/build.js",
    "build:prod": "NODE_ENV=production node scripts/build.js",
    "deploy:dev": "npm run build:dev && npx wrangler deploy --env dev",
    "deploy:prod": "npm run build:prod && npx wrangler deploy --env production",
    "update:wrangler": "ncu -u wrangler && npm install"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
